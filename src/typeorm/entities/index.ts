import { Profile } from './profile.entity';
import { User } from './user.entity';
import { Post } from './post.entity';

const entities = [User, Profile, Post];

export { User, Profile, Post };
export default entities;
